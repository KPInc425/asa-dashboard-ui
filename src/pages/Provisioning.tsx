import React from 'react';
import ServerProvisioner from '../components/ServerProvisioner';

const Provisioning: React.FC = () => {
  return <ServerProvisioner />;
};

export default Provisioning; 