/**
 * Demo Interceptor — Re-exports
 *
 * Split from the original demo-interceptor.ts (586 lines) into focused modules.
 */
export { installDemoInterceptor } from './interceptor';
export { getMockData } from './mock-data';
